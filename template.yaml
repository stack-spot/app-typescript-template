name: base-app-ts-template
description: Base template to work with typescript stack
types:
  - app-template
inputs:
  - label: Project name
    type: text
    name: project_name
    default: project-name
    global: true
  - label: Service name
    type: text
    name: service_name
    default: service-name
    global: true


displayName: Typescript + CDK

category: backend

compatibility:
  - NodeJS 14.x
  - CDK 1.x 

about: about.md

usage: usage.md

technologies: [ "Typescript", "CDK" ]